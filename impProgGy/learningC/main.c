#include <stdio.h>

int arrLength(int *arr)
{
  return (sizeof(arr) - sizeof(arr[0]));
}

int main()
{
  int arr[] = {34, 12, 21, 51, 48};

  for (int i = 0;)

    printf("Largest element: %p\n", largest);
}
